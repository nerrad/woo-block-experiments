/**
 * Internal dependencies
 */
import { Container } from '../../block-components/container';

export const CheckoutSubmitBlock = ( props ) => {
	return <Container className="checkout-submit-block" { ...props } />;
};
