/**
 * Internal dependencies
 */
import { Container } from '../../block-components/container';

export const PaymentsBlock = ( props ) => {
	return <Container className="payments-block" { ...props } />;
};
